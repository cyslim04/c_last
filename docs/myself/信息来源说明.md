# 信息来源说明

更新时间：2026-05-08

## 本次整理使用的来源

### 1. 用户提供截图

截图中可直接识别的信息包括：

- 当前角色显示为“系统管理员”
- 组织名显示为“Trust Work Lab”
- 邮箱显示为“admin@trustwork.demo”
- 顶部钱包按钮显示为“连接 MetaMask”
- 左侧导航中存在“个人中心”入口

### 2. 当前数据库

来源文件：

- `backend/data/trust_work.db`

已核实字段：

- `users` 表中的管理员账号资料
- `wallet_bindings` 表中的管理员钱包绑定记录

### 3. 后端源码

来源文件：

- `backend/src/server.c`

已核实内容：

- 管理员种子账号写入逻辑
- `admin` 对应演示密码为 `admin123`
- Bearer Token 使用 `demo-token-<用户ID>` 规则

### 4. 项目说明文档

来源文件：

- `README.md`

已核实内容：

- 管理员功能范围包含个人中心
- 顶部头像可进入个人中心

## 本次未纳入的信息

以下内容本次没有作为“已确认个人信息”写入总览：

- 未在截图或数据库中确认的真实姓名
- 不属于当前管理员账号的其他演示账号资料
- 无法确认是否仍生效的历史钱包地址
- 推测性信息或仅存在于旧种子代码、但与当前数据库不一致的信息

## 一致性说明

本次整理时发现，`backend/src/server.c` 中的管理员初始种子资料与 `backend/data/trust_work.db` 当前实际数据并不完全一致：

- 种子代码里管理员初始钱包为空
- 当前数据库里管理员已绑定钱包 `0x1be31a94361a391bbafb2a4ccd704f57dc04d4bb`

因此本目录优先采用“当前数据库中的实际值”，并在需要时保留源码作为历史来源说明。
