# 新增根级 gitignore 并整理忽略规则

## 变更概述
- 为仓库新增根级 [`.gitignore`](/C:/Users/yce/Desktop/c_last/.gitignore)，用于准备首次初始化 Git 并推送到 GitHub。
- 统一排除不应进入远端仓库的依赖目录、构建产物、后端运行数据、本机 IDE 文件和本地 Agent 私有配置。

## 具体调整
- 新增 [`.gitignore`](/C:/Users/yce/Desktop/c_last/.gitignore)：
  - 排除 `frontend/node_modules/`、`frontend/dist/`、`frontend/.vite/`
  - 排除 `contracts/node_modules/`、`contracts/artifacts/`、`contracts/cache/`、`contracts/coverage/`
  - 排除 `backend/data/`、`backend/uploads/` 以及 `backend/*.exe` 等本地编译产物
  - 排除 `.idea/`、`.vscode/`、`.claude/settings.local.json` 等本机环境文件
  - 排除 `contracts/.env` 等本地密钥文件，并保留 `contracts/.env.example`

## 验证
- 本次为 Git 忽略规则整理，未涉及前端、后端或合约逻辑修改。
- 后续可通过 `git status --short --ignored` 检查忽略结果是否符合预期。
- 当前日志总数由 37 增至 38，未达到每 5 篇日志后的同步节点，本轮未更新 `README.md`。
