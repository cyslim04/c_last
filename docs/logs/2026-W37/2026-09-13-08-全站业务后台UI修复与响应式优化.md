# 全站业务后台 UI 修复与响应式优化

## 变更内容

- 修复固定顶栏与历史 Hero 样式叠加造成的首屏大面积空白。
- 统一页面容器宽度、gutter、面板内边距、表单密度和移动端断点。
- 成员管理页明确区分当前登录管理员与正在维护的成员，优化资料与密码区域比例。
- 个人中心标题和表单层级收敛为基本资料、账号安全，减少展示型留白。
- 移除成员创建和密码重置中的默认演示密码，改为管理员明确输入。
- 用户菜单改为直接显示当前登录身份信息，顶部导航在中等宽度提前折叠。

## 验证

- `frontend/npm run build` 通过。
- 已使用静态检索确认源码业务组件不再引用默认 `member123` 密码。
- 未修改后端接口、数据库结构及运行时生成物。
